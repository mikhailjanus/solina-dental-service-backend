-- Dental Service DB init\n\nCREATE TABLE IF NOT EXISTS patients (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  patientFirstName VARCHAR(255) NOT NULL,\n  patientMiddleName VARCHAR(255),\n  patientLastName VARCHAR(255) NOT NULL,\n  email VARCHAR(255),\n  mobileNumber VARCHAR(20),\n  address TEXT,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP NULL\n);\n\nCREATE TABLE IF NOT EXISTS appointments (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  user_id INT,\n  patient_id INT,\n  service_id INT,\n  clinic_id INT,\n  appointment_date DATE,\n  appointment_time TIME,\n  status ENUM(pending, confirmed, completed, cancelled) DEFAULT pending,\n  notes TEXT,\n  total_amount DECIMAL(10,2),\n  payment_status ENUM(pending, paid) DEFAULT pending,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  FOREIGN KEY (patient_id) REFERENCES patients(id)\n);\n\nCREATE TABLE IF NOT EXISTS services (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  name VARCHAR(255) NOT NULL,\n  price DECIMAL(10,2)\n);\n\nCREATE TABLE IF NOT EXISTS clinics (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  name VARCHAR(255) NOT NULL,\n  address TEXT\n);\n\nINSERT INTO patients (patientFirstName, patientMiddleName, patientLastName, email, mobileNumber, address) VALUES\n  (John, A, Smith, john.smith@email.com, 555-0101, 123 Main Street),\n  (Janus, C, Solina, john.smith@email.com, 555-0101, 123 Main Street),\n  (Nocturnal, C, Solina, john.smith@email.com, 555-0101, 123 Main Street);\n\nINSERT INTO services (name, price) VALUES\n  (General Checkup, 50.00),\n  (Teeth Cleaning, 80.00),\n  (Dental Implants, 1500.00);